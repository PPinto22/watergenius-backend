/users							            # GET, POST
/users/{id}						          # GET, PUT, DEL

/properties                     # GET(owner/manager), POST(user)
/properties/{id}				        # GET, PUT, DEL
/properties/{id}/node			      # GET, POST, PUT
/properties/{id}/managers		    # GET
/properties/{id}/managers/{id}	# POST (Empty body), DEL

/spaces							            # GET(owner/manager, property), POST(property)
/spaces/{id}					          # GET, PUT, DEL
/spaces/{id}/restrictions		    # GET, POST
/spaces/{id}/restrictions/{id}	# PUT, DEL

/plants							            # GET (para auto-complete ??)

/subspaces						          # GET(property, space), POST(space)
/subspaces/{id}					        # GET, PUT, DEL

/plans							            # GET(property, spaces, subspace, time-interval), POST(subspace)
/plans/{id}						          # GET

/irrigations					          # GET(property, spaces, subspace, time-interval), POST(subspace)
/irrigations{id}				        # GET, PUT, DEL

/embeddedsys						        # GET(subspace), POST(subspace)
/embeddedsys/{id}				        # GET, PUT, DEL

/sensors						            # GET(subspace, embeddedsys), POST(embeddedsys)
/sensors/{id}					          # GET, PUT, DEL

/reads							            # GET(subspace, embeddedsys, sensor), POST(sensor)
/reads/{id}						          # GET

/warnings						            # GET(node/property), POST(property, error_code)
/warnings/{id}					        # GET, PUT, DEL

/node/{i}/poweroff				      # POST
